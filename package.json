{
    "name": "wedding-rsvp",
    "description": "AWS API and databse integration",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.334.0"
    },
    "devDependencies": {
        "mocha": "5.0.0"
    },
    "scripts": {
        "test": "mocha --recursive tests"
    }
}
